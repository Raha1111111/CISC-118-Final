# Start the number at 100
num = 100
# Continue looping as long as num is greater than or equal to 2
while num >= 2:
    # Print the current number
    print(num)
    # Decrement the number by 2 to ensure we're only printing even numbers
    num -= 2
